# vim: sts=2 ts=2 sw=2 et ai
helper:
  # WARNING, INFO, DEBUG, ERROR, CRITICAL
  loglevel = 'DEBUG'

  #netmask
  # [ ( netmask, gateway ), (netmask, gateway ), ]
  nets:
    {{public_iface}}: ['{{public_cidr}}', '{{public_gateway}}']
